import { Keypair } from "@solana/web3.js";
import { encode } from "bs58";

console.log(encode(Keypair.generate().secretKey));
console.log(encode(Keypair.generate().secretKey));
console.log(encode(Keypair.generate().secretKey));
console.log(encode(Keypair.generate().secretKey));
console.log(encode(Keypair.generate().secretKey));
